print("Ingresa cuantos numeros quieres de la serie de Fibonacci:");

scan(n);
n = tonum(n);

if (n <= 0) { print("Ingreso un numero mayor que cero"); }
elif (n == 1) { print(0); }
else {|
    a = 0;
    b = 1;

    print(a);
    print(b);

    for (i = 3; i <= n; i++) {
        c = a + b;
        print(c);
        a = b;
        b = c;
    }
}